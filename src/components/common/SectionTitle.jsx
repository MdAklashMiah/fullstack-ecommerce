import React from "react";

const SectionTitle = ({title}) => {
  return (
    <>
      <h2 className="text-center text-dark font-normal text-4xl">
        {title}
      </h2>
    </>
  );
};

export default SectionTitle;
